# Git {{
    alias g='git'
    alias got='git ' # Fix some fat fingers
    alias get='git '

    # Trying to drop these in favour of git aliases
    # There is a namespacing concern I want to nip in the bud.
    # ~/dotfiles/git/git-aliases

    # alias gs='git status '
    # alias ga='git add '
    # alias gb='git branch '
    # alias gc='git commit'
    # alias gd='git diff'
    # alias go='git checkout '
# }}

