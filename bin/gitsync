#!/usr/bin/env bash

# Check for fswatch is installed
# https://github.com/emcrisostomo/fswatch

# check that it is a git directory

# if there is no git directory then make one ?

# if it is then do a git pull with rebase

# Watch a directory

# When a file changes, store that file in a variable

# After an interval of two minutes/ one minute or something commit the changes with a time stamp?

# after another interval, push those changes up
